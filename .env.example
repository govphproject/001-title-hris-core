# Backend
PORT=8080
MONGO_URI=mongodb://localhost:27017/hris_dev
JWT_SECRET=replace-me-with-secure-secret

```bash
# Backend
PORT=8080
HRIS_JWT_SECRET=dev-jwt-secret

# Mongo configuration (used by backend and migration tool)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=hris
MONGO_COLLECTION=employees
MONGO_USERS_COLLECTION=users

# Admin bootstrap credentials (dev only)
HRIS_ADMIN_USER=admin
HRIS_ADMIN_PASSWORD=password

# Frontend
VITE_API_BASE_URL=http://localhost:8080/api

# When set to 1, contract tests will target an external server at http://localhost:8080/api
HRIS_TEST_EXTERNAL=0
```
